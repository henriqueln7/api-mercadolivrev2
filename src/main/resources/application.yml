spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/apimlv2
    username: postgres
    password: postgres
jwt:
  secret: jdk02kdadnjndiawd
  expTime: 86400